# Legado Web Docker 部署指南 (纯后端移植版)

本项目提供了将 Legado (阅读) 容器化部署至 Docker 平台的完整支持。

本方案是一套**纯后端移植版**，**不依赖任何虚拟机 (VM) 或 Android 模拟器**，也**不需要连接物理手机**。它利用 Robolectric 框架，在 Docker 内部的标准 JVM (OpenJDK 17) 环境下原生运行 Legado 的后端引擎（包括书源解析、书籍搜索、数据持久化等核心功能）。

---

## 1. 服务端口架构

容器内同时部署了 Web 服务及反向代理，暴露以下三个端口：
1. **端口 `4080` (原生全屏 Web UI)：** 提供构建自 `modules/web` 的 Vue 3 电子书网页版界面，已做 Web 适配及默认接口自动映射，拥有极佳的桌面及大屏阅读、书架管理、书源编辑体验。
2. **端口 `4081` (透传原生 Web 服务)：** Nginx 反向代理直接透传容器内运行的 Legado 后台 Web 服务（原本在手机端为 `1122` 端口）。访问此端口可以直接对接原生的 API 服务。
3. **端口 `4082` (透传 WebSocket 服务)：** 用作独立 WebSocket 通信代理，映射至容器内运行的 Legado 后端 WebSocket 端口（原本在手机端为 `1123` 端口），用于书籍搜索及调试的实时流传输。

---

## 2. 数据持久化 (Persistence)

容器内运行的 Legado SQLite 数据库已配置为持久化存储：
- 数据库文件保存在容器内的 `/storage/legado.db` 路径。
- 该路径默认挂载到宿主机当前目录下的 `./storage` 目录中。
- **重要：** 您的书架、书源、阅读记录等所有数据均会被妥善保存在宿主机上，即使容器被销毁重建，数据也不会丢失。

---

## 3. 使用 Docker Compose 部署 (推荐)

### 步骤一：启动服务
在项目根目录下，运行以下命令构建镜像并启动服务：

```bash
docker compose up -d --build
```

- 该命令会自动下载 Node 环境编译前端网页，并下载 JDK 环境配置 JVM 运行后端。
- 首次构建由于需要下载依赖并预编译，需要几分钟时间。

---

## 4. 使用 Docker CLI 部署

如果您不想使用 Docker Compose，也可以直接使用 Docker CLI 进行构建和运行：

### 步骤一：构建 Docker 镜像
在项目根目录下运行：

```bash
docker build -t legado-web .
```

### 步骤二：运行 Docker 容器
运行容器并映射对应端口，同时挂载数据持久化卷：

```bash
docker run -d \
  --name legado-web \
  --restart unless-stopped \
  -p 4080:4080 \
  -p 4081:4081 \
  -p 4082:4082 \
  -v ./storage:/storage \
  legado-web
```

---

## 5. 访问与使用

容器正常运行后，您可以通过浏览器访问以下地址：

*   **全屏 WebUI (推荐)：** `http://localhost:4080`
    *   此端口提供原生的全屏 Web 体验。
    *   在首次打开时，已作 Web 适配，默认会自动连接到同容器的 `http://localhost:4081` 代理接口。
    *   **无需手动设置后台地址，开箱即用。**
*   **直连原生服务端口：** `http://localhost:4081`
    *   此端口直接对应手机端原本开启 Web 服务后的 `1122` 端口效果。
    *   容器刚启动时，后端 JVM 初始化需要大约 10-20 秒。在此期间访问该端口会显示“服务正在启动”提示，随后会自动重定向至书架。
